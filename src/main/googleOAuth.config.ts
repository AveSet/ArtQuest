/**
 * Build-safe OAuth defaults.
 * Keep these empty in source control and inject values via environment variables.
 */
export const GOOGLE_OAUTH_CLIENT_ID = ''
export const GOOGLE_OAUTH_CLIENT_SECRET = ''
